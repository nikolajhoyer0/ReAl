
Rename: ρ

Usage:
Rename is used for creating a new table/relation with one of the columns/attributes in the old table/relation renamed.

Notation:
ρ (Column_name → New_column_name) Relation

Example:
Using the relation R(a1,a2,...,an), ρ (a2 → b2) R will return R(a1,b2,...,an).